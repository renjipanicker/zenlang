include gui::Window public;
namespace TextEdit;

public struct Definition : Window::Definition {
    auto multiline = false;
    auto readonly = false;
    auto wordwrap = true;
};

public function Window::Handle Create(const Window::Handle& parentWin, const Definition& defn) native;
public function void AppendText(const Window::Handle& wnd, const string& text) native;
public function void Clear(const Window::Handle& wnd) native;
public function string GetText(const Window::Handle& wnd) native;

public event(const Window::Handle& textedit) native => function void OnEnter();
