import z::Window public;
namespace z;

public interface MainFrame {
    struct Definition : Window::Definition {
        auto iconres = "";
    };

    function (widget wnd)Create(const Definition& defn) native;
};
