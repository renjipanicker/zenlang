include mainframe;
include button;

public main MyMain {
    local mainFrame = run MainFrame::Create( MainFrame::Definition{title:"test"; position:Window::Position{x:10;y:10;w:100;h:100;}; visible:true; } );
    local checkBtn = run Button::Create(mainFrame, Window::Definition{title:"Check";});
    return (0);
}
