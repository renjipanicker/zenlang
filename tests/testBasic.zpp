namespace TestBasic;

public routine int testNative(const string& s) native;

public routine int testRoutine(const int& x) {
    return 12345;
}

public function (int ri, string rs)testFunction(const int& x) {
    return (67890, "testfn");
}

public test testBasicExpr {
    local i = 0;
    local s = "xx";
    local e = (1 + 2) * 3;
    local n = (1 > 2) ? 1 : 2;
    local t = "yy %{x}\n" @ {"x":1};
    local l = [1,2,3];
    local d = ["a":1,"b":2,"c":3];
    local r = testRoutine(123);
    local f = testFunction(456);

    return (1);
}

public enum MyEnum {
    C1;
    C2;
};

public struct MyStruct {
    i = 0;
};

public function (int r)testFunction002(const int& x, const MyStruct& myStruct, const MyEnum& myEnum) {
    local v = x;
    local m = myStruct.i;
    return (67890);
}

public test testFunction001 {
    print "yy %{x}\n" @ {"x":1};
//    local myStruct = MyStruct[];
//    local c = testFunction(321);
    return (1);
}
